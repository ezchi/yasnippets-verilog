# -*- mode: snippet -*-
# name: opt-wrapper
# --
module ${1:}_wrapper(
    input  logic clk,
    input  logic rst,
    $0
);

    import $1_pkg::*;

    $1
    inner(
        .iClk(clk),
        .iRst(rst),
    );

    always_comb begin
    end
endmodule : $1_wrapper
